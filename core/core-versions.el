;;; core-versions.el -*- lexical-binding: t; -*-

(message "core-versions")

(defconst warmacs-version          "0.0.0" "Warmacs version.")
(defconst warmacs-emacs-min-version   "27.1" "Minimal version of Emacs.")

(provide 'core-versions)
